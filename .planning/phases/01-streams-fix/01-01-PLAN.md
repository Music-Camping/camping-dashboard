---
phase: 01-streams-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/presentation-mode/company-display.tsx
  - components/dashboard/dashboard-client.tsx
  - components/presentation-mode/performer-presentation.tsx
autonomous: true
requirements:
  - STRM-01
  - STRM-02

must_haves:
  truths:
    - "The aggregated streams total in company-display.tsx does not include YouTube views — only Spotify ranking streams contribute"
    - "The per-performer streams total in dashboard-client.tsx does not include YouTube views — only Spotify ranking streams contribute"
    - "The streams delta shown in the performer presentation card is undefined (no delta badge), not a YouTube-sourced number"
    - "Both MetricCard instances that show streams are labeled 'Spotify Streams', not 'Streams'"
    - "YouTube views remain visible in their own separate MetricCard (label='Views') — they are not removed"
  artifacts:
    - path: "components/presentation-mode/company-display.tsx"
      provides: "Aggregated streams total and delta with YouTube removed; label updated"
      contains: "Spotify Streams"
    - path: "components/dashboard/dashboard-client.tsx"
      provides: "Per-performer streams total with YouTube block removed; streamsDelta prop set to undefined"
      contains: "Total streams (Spotify track streams only)"
    - path: "components/presentation-mode/performer-presentation.tsx"
      provides: "Streams MetricCard label updated"
      contains: "Spotify Streams"
  key_links:
    - from: "company-display.tsx (aggregated useMemo)"
      to: "MetricCard label='Spotify Streams'"
      via: "aggregated.streams value, Spotify rankings only"
      pattern: "Spotify Streams"
    - from: "dashboard-client.tsx (tvTotalStreams useMemo)"
      to: "PerformerPresentation streamsDelta prop"
      via: "streamsDelta={undefined}"
      pattern: "streamsDelta=\\{undefined\\}"
---

<objective>
Fix the streams metric so it reflects Spotify-only data in both presentation views, and update labels to say "Spotify Streams".

Purpose: Stakeholders currently see an inflated streams number that wrongly includes YouTube views. This fix makes the streams metric accurate (Spotify only) and honest in its labeling.
Output: Three modified source files. No new files, no new libraries, no schema changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Key types and contracts from the files being modified. Extracted so the executor does not need to explore. -->

<interfaces>
<!-- From company-display.tsx — aggregated streams useMemo (lines ~280–350) -->
The useMemo builds an `aggregated` object. The YouTube block currently:
```typescript
if (data.youtube.views?.latest) {
  totalViews += data.youtube.views.latest;
  totalStreams += data.youtube.views.latest; // REMOVE
  hasViews = true;
  hasStreams = true;                          // REMOVE
}
```
After fix the YouTube block should only affect `totalViews` and `hasViews`.

<!-- From company-display.tsx — deltas useMemo (lines ~398–425) -->

The useMemo currently assigns YouTube views delta to BOTH viewsDelta and streamsDelta:

```typescript
viewsDelta += getMetricDelta(data.youtube?.views, period) ?? 0;
streamsDelta += getMetricDelta(data.youtube?.views, period) ?? 0; // REMOVE
```

After fix: only the `viewsDelta` line remains. The `streamsDelta` variable stays 0, and
`streamsDelta || undefined` in the return correctly produces `undefined` — no delta badge shown.

<!-- From dashboard-client.tsx — tvTotalStreams useMemo (lines ~238–265) -->

Current code adds YouTube views to the Spotify-only total:

```typescript
// YouTube views
const ytViews = presentation.currentPerformer
  ? initialData?.[presentation.currentPerformer]?.youtube?.views?.latest
  : undefined;
if (ytViews) {
  total += ytViews;
  hasData = true;
}
```

After fix: the entire "YouTube views" block (comment + const + if) is removed.

<!-- From dashboard-client.tsx — PerformerPresentation JSX (line ~492) -->

Current prop:

```tsx
streamsDelta = { tvViewsDelta };
```

After fix:

```tsx
streamsDelta = { undefined };
```

<!-- Comment at dashboard-client.tsx line ~237 -->

Before: `// Total streams (Spotify track streams + YouTube views)`
After: `// Total streams (Spotify track streams only)`

<!-- Label targets -->

company-display.tsx ~line 469: `label="Streams"` → `label="Spotify Streams"`
performer-presentation.tsx ~line 210: `label="Streams"` → `label="Spotify Streams"`

<!-- Icon target (Claude discretion) -->

company-display.tsx ~line 473: `icons={[spotifyIcon, youtubeIcon]}` → `icons={[spotifyIcon]}`
YouTube no longer contributes to streams so showing the YouTube icon here is misleading.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove YouTube from streams calculations (D-01, D-02)</name>
  <files>
    components/presentation-mode/company-display.tsx
    components/dashboard/dashboard-client.tsx
  </files>
  <read_first>
    - components/presentation-mode/company-display.tsx (read lines 318–430 to see both useMemos before editing)
    - components/dashboard/dashboard-client.tsx (read lines 237–270 and lines 485–500 to see tvTotalStreams useMemo and the JSX prop before editing)
  </read_first>
  <action>
**File 1: components/presentation-mode/company-display.tsx**

Edit 1 — Remove YouTube contribution to streams total (Bug 1).
In the `aggregated` useMemo, inside the `if (data.youtube.views?.latest)` block, remove exactly these two lines:

```
  totalStreams += data.youtube.views.latest;
  hasStreams = true;
```

The block must be left with only:

```typescript
if (data.youtube.views?.latest) {
  totalViews += data.youtube.views.latest;
  hasViews = true;
}
```

Edit 2 — Remove YouTube contribution to streams delta (Bug 2).
In the `deltas` useMemo, remove exactly this line:

```
  streamsDelta += getMetricDelta(data.youtube?.views, period) ?? 0;
```

Leave the `viewsDelta` line unchanged. The `streamsDelta` variable stays initialized to 0 and the return `streamsDelta || undefined` will correctly return `undefined` — no delta badge shown. Do NOT add any Spotify delta calculation; that is out of scope per D-02.

**File 2: components/dashboard/dashboard-client.tsx**

Edit 3 — Remove YouTube block from tvTotalStreams useMemo (Bug 3).
In the `tvTotalStreams` useMemo, remove the entire YouTube views block including its comment:

```
    // YouTube views
    const ytViews = presentation.currentPerformer
      ? initialData?.[presentation.currentPerformer]?.youtube?.views?.latest
      : undefined;
    if (ytViews) {
      total += ytViews;
      hasData = true;
    }
```

The useMemo dependencies array references `initialData`. After removing the YouTube block, `initialData` is no longer used inside the useMemo. Remove `initialData` from the dependency array too (the array currently reads `[spotifyData, presentation.currentPerformer, initialData]` — change to `[spotifyData, presentation.currentPerformer]`).

Edit 4 — Update tvTotalStreams comment.
Change the comment directly above the `tvTotalStreams` useMemo:

```
  // Total streams (Spotify track streams + YouTube views)
```

to:

```
  // Total streams (Spotify track streams only)
```

Edit 5 — Set streamsDelta prop to undefined in JSX (Fix 4 / D-02).
In the `PerformerPresentation` JSX, change:

```tsx
streamsDelta = { tvViewsDelta };
```

to:

```tsx
streamsDelta = { undefined };
```

After both files are saved, run: `npm run type-check`
</action>
<verify>
<automated>npm run type-check</automated>
</verify>
<acceptance_criteria> - grep "totalStreams += data.youtube" components/presentation-mode/company-display.tsx returns no matches - grep "hasStreams = true" components/presentation-mode/company-display.tsx — only one match, inside the Spotify block (not inside the YouTube block) - grep "streamsDelta += getMetricDelta" components/presentation-mode/company-display.tsx returns no matches - grep "ytViews" components/dashboard/dashboard-client.tsx returns no matches - grep "total \+= ytViews" components/dashboard/dashboard-client.tsx returns no matches - grep "Spotify track streams only" components/dashboard/dashboard-client.tsx returns exactly 1 match - grep "streamsDelta={undefined}" components/dashboard/dashboard-client.tsx returns exactly 1 match - grep "initialData" components/dashboard/dashboard-client.tsx in the tvTotalStreams useMemo dependency array returns no match (initialData removed from deps) - npm run type-check exits with code 0
</acceptance_criteria>
<done>YouTube views are excluded from both streams total and streams delta in both rendering paths. Type-check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update streams metric labels and icon (D-04, discretion)</name>
  <files>
    components/presentation-mode/company-display.tsx
    components/presentation-mode/performer-presentation.tsx
  </files>
  <read_first>
    - components/presentation-mode/company-display.tsx (read lines 465–480 to see the streams MetricCard label and icons before editing)
    - components/presentation-mode/performer-presentation.tsx (read lines 205–225 to see the streams MetricCard label before editing)
  </read_first>
  <action>
**File 1: components/presentation-mode/company-display.tsx**

Edit 1 — Update streams label (D-04 / STRM-02).
In the streams MetricCard JSX, change:

```tsx
label = "Streams";
```

to:

```tsx
label = "Spotify Streams";
```

This is on the MetricCard that renders `aggregated.streams`.

Edit 2 — Remove YouTube icon from streams card (Claude's discretion).
On the same MetricCard, change:

```tsx
icons={[spotifyIcon, youtubeIcon]}
```

to:

```tsx
icons={[spotifyIcon]}
```

YouTube no longer contributes to the streams total, so displaying the YouTube icon on this card is misleading. The YouTube icon remains on the Views MetricCard below it unchanged.

**File 2: components/presentation-mode/performer-presentation.tsx**

Edit 3 — Update streams label (D-04 / STRM-02).
In the streams MetricCard JSX (the one rendering `totalStreams`), change:

```tsx
label = "Streams";
```

to:

```tsx
label = "Spotify Streams";
```

After both files are saved, run the full validation suite: `npm run type-check && npm run lint && npm run build`
</action>
<verify>
<automated>npm run type-check && npm run lint && npm run build</automated>
</verify>
<acceptance_criteria> - grep -n '"Streams"' components/presentation-mode/company-display.tsx returns no matches (label replaced) - grep -n '"Streams"' components/presentation-mode/performer-presentation.tsx returns no matches (label replaced) - grep "Spotify Streams" components/presentation-mode/company-display.tsx returns exactly 1 match - grep "Spotify Streams" components/presentation-mode/performer-presentation.tsx returns exactly 1 match - grep "icons={\[spotifyIcon, youtubeIcon\]}" components/presentation-mode/company-display.tsx returns no matches (YouTube icon removed from streams card) - grep "youtubeIcon" components/presentation-mode/company-display.tsx still returns at least 1 match (YouTube icon still present on the Views MetricCard — it was not removed globally) - npm run type-check exits 0 - npm run lint exits 0 (or with ≤10 warnings, all pre-existing) - npm run build exits 0
</acceptance_criteria>
<done>Both streams MetricCards show "Spotify Streams". The company streams card no longer shows the YouTube icon. Full build pipeline passes.</done>
</task>

</tasks>

<verification>
Full build pipeline must pass after Task 2:
```
npm run type-check && npm run lint && npm run build
```

Manual browser verification (start dev server with `npm run dev`):

1. Activate presentation mode
2. View a performer slide — the streams MetricCard must show "Spotify Streams" as label
3. View the company aggregate slide — the streams MetricCard must show "Spotify Streams" as label
4. Confirm the streams value no longer includes YouTube view counts (compare against Spotify track streams sum only)
5. Confirm YouTube views still appear in a separate "Views" MetricCard — they are not removed
6. Confirm no delta badge appears on the streams card in the performer view (streamsDelta is undefined)
   </verification>

<success_criteria>

1. The streams number matches Spotify track stream counts only — YouTube views are not included (STRM-01)
2. Both streams MetricCards are labeled "Spotify Streams" — not "Streams" (STRM-02)
3. YouTube views remain visible as a separate "Views" MetricCard (D-03 preserved)
4. Full pipeline passes: npm run type-check && npm run lint && npm run build
   </success_criteria>

<output>
After completion, create `.planning/phases/01-streams-fix/01-01-SUMMARY.md` using the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
