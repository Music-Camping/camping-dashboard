---
phase: 02-tv-grid-layout
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/presentation-mode/performer-presentation.tsx
autonomous: true
requirements: [GRID-01, GRID-03, GRID-04, GRID-05]

must_haves:
  truths:
    - "Performer page left half shows 2x3 metric card grid filling full container height"
    - "Performer page right half shows Top 10 tracks list filling full container height"
    - "Left and right halves split 50/50 via grid-cols-2"
    - "All text uses clamp() typography — no fixed text-sm or text-xs classes"
    - "Metric card labels have minimum 24px floor via clamp(1.5rem, 1.3vw, 2rem)"
  artifacts:
    - path: "components/presentation-mode/performer-presentation.tsx"
      provides: "Restructured performer presentation with two-column grid layout and clamp() typography"
      contains: "grid-cols-2"
  key_links:
    - from: "performer-presentation.tsx"
      to: "dashboard-client.tsx"
      via: "rendered inside absolute inset-0 motion.div — inherits h-full from parent"
      pattern: "h-full"
    - from: "MetricCard"
      to: "grid cell"
      via: "h-full fills grid-rows-3 cell"
      pattern: "h-full.*overflow-hidden"
---

<objective>
Adapt performer-presentation.tsx to render as a two-column layout: left half = 2x3 metric cards grid, right half = Top 10 Spotify tracks list. Apply clamp() typography to all text elements. Both halves fill 100% of the container height.

Purpose: Implement the performer page layout per D-04 through D-06 with proportional text scaling per GRID-04 and GRID-05.
Output: Restructured performer presentation component
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-tv-grid-layout/02-CONTEXT.md
@.planning/phases/02-tv-grid-layout/02-UI-SPEC.md
@.planning/phases/02-tv-grid-layout/02-RESEARCH.md
@.planning/phases/02-tv-grid-layout/02-01-SUMMARY.md

<interfaces>
<!-- Current component structure to adapt -->

From components/presentation-mode/performer-presentation.tsx:

```typescript
interface PerformerPresentationProps {
  performerName: string;
  rankings: SpotifyRanking[];
  cityData?: { entries: CityEntry[] } | null;
  bannerUrl?: string | null;
  totalStreams?: number;
  youtubeVideos?: number;
  youtubeViews?: number;
  totalFollowers?: number;
  monthlyListeners?: number;
  streamsDelta?: number;
  videosDelta?: number;
  viewsDelta?: number;
  followersDelta?: number;
  listenersDelta?: number;
  hasSpotify?: boolean;
  hasYoutube?: boolean;
  hasTiktok?: boolean;
}
```

Current internal layout (line 205):

```typescript
<div className="grid flex-1 grid-cols-[2fr_3fr] gap-6 overflow-hidden">
  <div className="grid grid-cols-2 grid-rows-3 gap-3.5">
    {/* MetricCards */}
  </div>
  <div className="flex flex-col overflow-hidden">
    {/* Top 10 tracks */}
  </div>
</div>
```

Typography tiers from UI-SPEC (must replace all fixed text sizes):

- display: text-[clamp(1.5rem,3vw,4rem)] font-black tabular-nums
- heading: text-[clamp(1.5rem,1.3vw,2rem)] font-medium
- body: text-[clamp(0.875rem,1vw,1.125rem)] font-medium
- caption: text-[clamp(0.75rem,0.9vw,1rem)] font-medium
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure performer-presentation layout to two-column grid with clamp() typography</name>
  <files>components/presentation-mode/performer-presentation.tsx</files>
  <read_first>
    - components/presentation-mode/performer-presentation.tsx (full file)
    - .planning/phases/02-tv-grid-layout/02-UI-SPEC.md (Layout Architecture: Performer Page sections, Typography section)
    - .planning/phases/02-tv-grid-layout/02-RESEARCH.md (Pattern 1-2, Pattern 4, Anti-patterns)
  </read_first>
  <action>
    Modify performer-presentation.tsx to implement the new two-column layout and clamp() typography. The component props interface stays UNCHANGED. All internal helper components (SpotifyIcon, YouTubeIcon, TikTokIcon, StackedIcons) stay UNCHANGED.

    **Layout changes (per D-04, D-05, D-06):**

    1. Change the content grid (currently line 205):
       - FROM: `grid flex-1 grid-cols-[2fr_3fr] gap-6 overflow-hidden`
       - TO: `grid h-full grid-cols-2 gap-[1vh] overflow-hidden`
       This makes left and right halves equal width (50/50 per D-01) instead of 2:3 ratio.

    2. Change the left metric grid (currently line 207):
       - FROM: `grid grid-cols-2 grid-rows-3 gap-3.5`
       - TO: `grid grid-cols-2 grid-rows-3 h-full gap-[1vh] overflow-hidden`
       Add `h-full` and `overflow-hidden` so grid fills parent and rows constrain to 1/3 each.

    3. Change the right tracks container (currently line 340):
       - FROM: `flex flex-col overflow-hidden`
       - TO: `flex flex-col h-full overflow-hidden`
       Add `h-full` to fill parent.

    4. Change the outermost content wrapper (currently line 203):
       - FROM: `relative z-10 flex h-full flex-col px-5 py-4`
       - TO: `relative z-10 flex h-full flex-col gap-[1vh] overflow-hidden`
       Remove px-5 py-4 (padding is now via p-[1vh] from the parent motion.div in dashboard-client). Remove flex-col since the grid is the only child and should fill all space. Actually keep flex-col but ensure the grid child gets flex-1.

       More precisely:
       - FROM: `relative z-10 flex h-full flex-col px-5 py-4`
       - TO: `relative z-10 h-full overflow-hidden`
       Remove the flex wrapper since the grid IS the content. The grid child gets `h-full` directly.

    **Typography changes (per GRID-04, GRID-05, D-16):**

    Replace every fixed text size class with clamp() equivalents from the UI-SPEC 4-tier type scale:

    5. **MetricCard component** (internal, around line 81-117):
       - Label: `text-sm font-medium text-white/50` -> `text-[clamp(1.5rem,1.3vw,2rem)] font-medium text-white/50` (heading tier)
       - Value: `text-3xl font-black text-white tabular-nums` -> `text-[clamp(1.5rem,3vw,4rem)] font-black text-white tabular-nums` (display tier)
       - Card padding: `p-5` -> `p-[1.5vh]` (viewport-relative)
       - Add `h-full` to the MetricCard root motion.div so it fills its grid cell

    6. **DeltaBadge component** (around line 67-78):
       - `text-xl font-black tabular-nums` -> `text-[clamp(1.5rem,3vw,4rem)] font-black tabular-nums` (display tier)

    7. **Top Cities card** (around line 262-304):
       - Label "Top Cidades": `text-sm font-medium text-white/50` -> `text-[clamp(1.5rem,1.3vw,2rem)] font-medium text-white/50` (heading tier)
       - Rank number: `text-xs font-bold text-white/30` -> `text-[clamp(0.75rem,0.9vw,1rem)] font-bold text-white/30` (caption tier)
       - City name: `text-sm font-medium text-white/80` -> `text-[clamp(0.75rem,0.9vw,1rem)] font-medium text-white/80` (caption tier)
       - City value: `text-sm font-bold text-amber-400 tabular-nums` -> `text-[clamp(0.75rem,0.9vw,1rem)] font-bold text-amber-400 tabular-nums` (caption tier)
       - Card padding: `p-4` -> `p-[1.5vh]` (viewport-relative)
       - Add `h-full` to the motion.div root of this card

    8. **Track list items** (around line 344-384):
       - Track name: `text-sm font-semibold text-white/90` -> `text-[clamp(0.875rem,1vw,1.125rem)] font-medium text-white/90` (body tier)
       - Artist name: `text-xs text-white/45` -> `text-[clamp(0.75rem,0.9vw,1rem)] font-medium text-white/45` (caption tier)
       - Stream count: `text-sm font-bold text-white/70 tabular-nums` -> `text-[clamp(0.875rem,1vw,1.125rem)] font-medium text-white/70 tabular-nums` (body tier)
       - Track container: `p-2.5` -> `p-[1.5vh]` (viewport-relative)

    9. **Empty state text** (line 388):
       - `text-sm text-white/40` -> `text-[clamp(0.875rem,1vw,1.125rem)] font-medium text-white/40` (body tier)

    **Overflow safety:**
    - Add `overflow-hidden` to every container that could overflow (metric card roots, track list container)
    - Add `min-h-0` on any flex children inside grid cells to prevent implicit minimum sizing

  </action>
  <verify>
    <automated>cd /home/wicar/personal/camping-dashboard && npx tsc --noEmit 2>&1 | head -20 && echo "---" && grep -c "text-sm\|text-xs\|text-xl\|text-2xl\|text-3xl" components/presentation-mode/performer-presentation.tsx</automated>
  </verify>
  <acceptance_criteria>
    - performer-presentation.tsx contains `grid-cols-2 grid-rows-3 h-full gap-[1vh] overflow-hidden` (left metric grid)
    - performer-presentation.tsx contains `grid h-full grid-cols-2 gap-[1vh] overflow-hidden` (main two-column split)
    - performer-presentation.tsx contains `text-[clamp(1.5rem,1.3vw,2rem)]` (heading tier for labels) at least 2 times
    - performer-presentation.tsx contains `text-[clamp(1.5rem,3vw,4rem)]` (display tier for values) at least 2 times
    - performer-presentation.tsx contains `text-[clamp(0.875rem,1vw,1.125rem)]` (body tier for tracks) at least 2 times
    - performer-presentation.tsx contains `text-[clamp(0.75rem,0.9vw,1rem)]` (caption tier) at least 3 times
    - performer-presentation.tsx contains ZERO instances of `text-sm` (all replaced with clamp)
    - performer-presentation.tsx contains ZERO instances of `text-xs` (all replaced with clamp)
    - performer-presentation.tsx contains ZERO instances of `text-3xl` or `text-2xl` (replaced with display clamp)
    - performer-presentation.tsx contains `p-[1.5vh]` for card padding
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Performer presentation uses 50/50 two-column grid with metric cards left and tracks right; all typography uses clamp() with 24px minimum floor on metric labels</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes
2. `npm run lint` passes
3. `npm run build` succeeds — Tailwind JIT includes all clamp() arbitrary values and gap-[1vh]
4. grep confirms zero instances of text-sm, text-xs, text-xl, text-2xl, text-3xl in performer-presentation.tsx
5. grep confirms clamp values present for all 4 typography tiers
</verification>

<success_criteria>

- Performer page has 50/50 two-column layout (grid-cols-2)
- Left half: 2x3 metric cards filling container height
- Right half: Top 10 tracks filling container height
- All text uses clamp() — no fixed Tailwind text size classes
- Metric labels have 1.5rem (24px) minimum floor
- No overflow or scroll on any container
  </success_criteria>

<output>
After completion, create `.planning/phases/02-tv-grid-layout/02-02-SUMMARY.md`
</output>
