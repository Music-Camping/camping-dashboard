---
phase: 02-tv-grid-layout
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/presentation-mode/company-display.tsx
autonomous: true
requirements: [GRID-01, GRID-03, GRID-04, GRID-05]

must_haves:
  truths:
    - "Company page left half shows 2x3 metric card grid filling full container height"
    - "Company page right half shows performer cards stacked vertically, each exactly 1/3 height"
    - "Left and right halves split 50/50 via grid-cols-2"
    - "Carousel auto-rotates pages of 3 performers when >3 exist"
    - "All text uses clamp() typography — no fixed text-sm or text-xs classes"
    - "Performer cards are always 1/3 height even with fewer than 3 performers"
  artifacts:
    - path: "components/presentation-mode/company-display.tsx"
      provides: "Restructured company display with two-column grid layout, fixed-height artist cards, and clamp() typography"
      contains: "grid-cols-2"
  key_links:
    - from: "company-display.tsx"
      to: "dashboard-client.tsx"
      via: "rendered inside absolute inset-0 motion.div — inherits h-full from parent"
      pattern: "h-full"
    - from: "ArtistCard wrappers"
      to: "right half flex container"
      via: "h-1/3 shrink-0 — always 1/3 of parent regardless of card count"
      pattern: "h-1/3"
---

<objective>
Adapt company-display.tsx to render as a two-column layout: left half = 2x3 metric cards grid, right half = performer artist cards (1/3 height each, carousel if >3 performers). Apply clamp() typography to all text elements.

Purpose: Implement the company page layout per D-07 through D-15 with proportional text scaling per GRID-04 and GRID-05. Artist cards use h-1/3 per D-15 for consistent sizing.
Output: Restructured company display component
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-tv-grid-layout/02-CONTEXT.md
@.planning/phases/02-tv-grid-layout/02-UI-SPEC.md
@.planning/phases/02-tv-grid-layout/02-RESEARCH.md
@.planning/phases/02-tv-grid-layout/02-01-SUMMARY.md

<interfaces>
<!-- Current component structure to adapt -->

From components/presentation-mode/company-display.tsx:

```typescript
interface CompanyDisplayProps {
  performers: string[];
  rotationInterval: number;
  initialData?: DashboardResponse | null;
  spotifyData?: SpotifyMetrics;
  period: PeriodFilter;
  bannerUrl?: string;
}
```

Current internal layout (line 461):

```typescript
<div className="grid flex-1 grid-cols-[2fr_3fr] gap-6 overflow-hidden">
  <div className="grid grid-cols-2 grid-rows-3 gap-3.5">
    {/* MetricCards */}
  </div>
  <div className="relative flex flex-col gap-3.5 overflow-hidden">
    {/* Artist cards carousel */}
  </div>
</div>
```

Current ArtistCard wrapper (line 578):

```typescript
{currentPerformers.map((performer, idx) => (
  <ArtistCard key={performer} ... delay={idx * 0.08} />
))}
```

ArtistCard root is: `motion.div className="relative flex-1 overflow-hidden rounded-2xl ..."`

Typography tiers from UI-SPEC:

- display: text-[clamp(1.5rem,3vw,4rem)] font-black tabular-nums
- heading: text-[clamp(1.5rem,1.3vw,2rem)] font-medium
- body: text-[clamp(0.875rem,1vw,1.125rem)] font-medium
- caption: text-[clamp(0.75rem,0.9vw,1rem)] font-medium
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure company-display layout to two-column grid with h-1/3 artist cards</name>
  <files>components/presentation-mode/company-display.tsx</files>
  <read_first>
    - components/presentation-mode/company-display.tsx (full file)
    - .planning/phases/02-tv-grid-layout/02-UI-SPEC.md (Layout Architecture: Company Page sections, Typography section)
    - .planning/phases/02-tv-grid-layout/02-RESEARCH.md (Pattern 3: Performer Artist Cards, Pattern 4: clamp() Typography, Pitfall 4: h-1/3 vs flex-1)
  </read_first>
  <action>
    Modify company-display.tsx to implement the new two-column layout, fixed-height artist cards, and clamp() typography. The component props interface stays UNCHANGED. All data aggregation logic (aggregated, topCities, deltas useMemos) stays UNCHANGED. Carousel logic (pages, currentPage, totalPages, useEffect auto-rotation) stays UNCHANGED.

    **Layout changes (per D-07, D-08, D-09, D-15):**

    1. Change the content grid (currently line 461):
       - FROM: `grid flex-1 grid-cols-[2fr_3fr] gap-6 overflow-hidden`
       - TO: `grid h-full grid-cols-2 gap-[1vh] overflow-hidden`
       Equal 50/50 split instead of 2:3 ratio. Use `h-full` instead of `flex-1`.

    2. Change the left metric grid (currently line 463):
       - FROM: `grid grid-cols-2 grid-rows-3 gap-3.5`
       - TO: `grid grid-cols-2 grid-rows-3 h-full gap-[1vh] overflow-hidden`
       Add `h-full` and `overflow-hidden`.

    3. Change the right artist cards container (currently line 561):
       - FROM: `relative flex flex-col gap-3.5 overflow-hidden`
       - TO: `relative h-full overflow-hidden flex flex-col gap-[1vh]`
       Add `h-full`, change `gap-3.5` to `gap-[1vh]`.

    4. Change artist card wrappers — currently ArtistCard has `className="relative flex-1 overflow-hidden rounded-2xl..."` as its root. Per D-15, each card must be exactly 1/3 of container height regardless of how many cards exist.

       Wrap each ArtistCard in a div with fixed height:
       ```typescript
       {currentPerformers.map((performer, idx) => {
         const data = initialData?.[performer];
         return (
           <div
             key={performer}
             className="h-1/3 shrink-0 min-h-0 overflow-hidden rounded-2xl"
           >
             <ArtistCard
               name={performer}
               bannerUrl={data?.files?.banner ?? null}
               profileUrl={data?.files?.profile ?? null}
               spotifyFollowers={data?.spotify?.followers.latest}
               youtubeSubscribers={data?.youtube?.followers.latest}
               monthlyListeners={data?.spotify?.monthly_listeners?.latest}
               delay={idx * 0.08}
             />
           </div>
         );
       })}
       ```

    5. Change ArtistCard root element (currently line 177):
       - FROM: `relative flex-1 overflow-hidden rounded-2xl bg-white/[0.03] shadow-lg backdrop-blur-md`
       - TO: `relative h-full overflow-hidden bg-white/[0.03] shadow-lg backdrop-blur-md`
       Remove `flex-1` (parent div handles sizing), remove `rounded-2xl` (parent wrapper has it), add `h-full`.

    6. Change the outermost content wrapper (currently line 460):
       - FROM: `relative z-10 flex h-full flex-col px-5 py-4`
       - TO: `relative z-10 h-full overflow-hidden`
       Remove flex wrapper and padding (padding comes from parent motion.div in dashboard-client).

    7. Change the carousel page indicator badge (currently line 564):
       - FROM: `text-xs font-medium`
       - TO: `text-[clamp(0.75rem,0.9vw,1rem)] font-medium` (caption tier)
       - Keep: `px-2 py-1` fixed padding per UI-SPEC spacing exceptions

    **Typography changes (per GRID-04, GRID-05, D-16):**

    Replace every fixed text size class with clamp() equivalents:

    8. **MetricCard component** (internal, around line 113-150):
       - Label: `text-sm font-medium text-white/50` -> `text-[clamp(1.5rem,1.3vw,2rem)] font-medium text-white/50` (heading tier)
       - Value: `text-2xl font-black text-white tabular-nums` -> `text-[clamp(1.5rem,3vw,4rem)] font-black text-white tabular-nums` (display tier)
       - Card padding: `p-4` -> `p-[1.5vh]` (viewport-relative)
       - Add `h-full` to the MetricCard root motion.div

    9. **DeltaBadge component** (around line 68-79):
       - `text-xl font-black tabular-nums` -> `text-[clamp(1.5rem,3vw,4rem)] font-black tabular-nums` (display tier)

    10. **Top Cities card** (around line 514-557):
        - Label "Top Cidades": `text-sm font-medium text-white/50` -> `text-[clamp(1.5rem,1.3vw,2rem)] font-medium text-white/50` (heading tier)
        - Rank number: `text-xs font-bold text-white/30` -> `text-[clamp(0.75rem,0.9vw,1rem)] font-bold text-white/30` (caption tier)
        - City name: `text-sm font-medium text-white/80` -> `text-[clamp(0.75rem,0.9vw,1rem)] font-medium text-white/80` (caption tier)
        - City value: `text-sm font-bold text-amber-400 tabular-nums` -> `text-[clamp(0.75rem,0.9vw,1rem)] font-bold text-amber-400 tabular-nums` (caption tier)
        - Card padding: `p-4` -> `p-[1.5vh]`
        - Add `h-full` to the motion.div root

    11. **ArtistCard component** (around line 154-237):
        - Performer name: `text-2xl font-black tracking-tight text-white` -> `text-[clamp(1.5rem,3vw,4rem)] font-black tracking-tight text-white` (display tier)
        - Quick stats container: `text-sm text-white/60` -> `text-[clamp(0.75rem,0.9vw,1rem)] font-medium text-white/60` (caption tier)
        - Card internal padding: `p-5` -> `p-[1.5vh]`
        - Profile image size: keep `size-14` (fixed — image asset)

    **Overflow safety:**
    - `overflow-hidden` on all grid cells, flex containers, card roots
    - `min-h-0` on flex children inside grid cells

  </action>
  <verify>
    <automated>cd /home/wicar/personal/camping-dashboard && npx tsc --noEmit 2>&1 | head -20 && echo "---" && grep -c "text-sm\|text-xs\|text-xl\|text-2xl\|text-3xl" components/presentation-mode/company-display.tsx</automated>
  </verify>
  <acceptance_criteria>
    - company-display.tsx contains `grid h-full grid-cols-2 gap-[1vh] overflow-hidden` (main two-column split)
    - company-display.tsx contains `grid grid-cols-2 grid-rows-3 h-full gap-[1vh] overflow-hidden` (left metric grid)
    - company-display.tsx contains `h-1/3 shrink-0 min-h-0 overflow-hidden` (artist card wrapper)
    - company-display.tsx contains `text-[clamp(1.5rem,1.3vw,2rem)]` (heading tier) at least 2 times
    - company-display.tsx contains `text-[clamp(1.5rem,3vw,4rem)]` (display tier) at least 3 times
    - company-display.tsx contains `text-[clamp(0.75rem,0.9vw,1rem)]` (caption tier) at least 4 times
    - company-display.tsx contains ZERO instances of `text-sm` (all replaced with clamp)
    - company-display.tsx contains ZERO instances of `text-xs` (all replaced with clamp)
    - company-display.tsx contains ZERO instances of `text-2xl` or `text-xl` (replaced with display clamp)
    - company-display.tsx contains `p-[1.5vh]` for card padding
    - company-display.tsx does NOT contain `flex-1` on ArtistCard root (replaced with h-full)
    - company-display.tsx DOES contain `h-1/3` for artist card wrappers
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>Company display uses 50/50 two-column grid with metric cards left and artist cards right; artist cards always 1/3 height per D-15; all typography uses clamp()</done>
</task>

</tasks>

<verification>
1. `npm run type-check` passes
2. `npm run lint` passes
3. `npm run build` succeeds — Tailwind JIT includes all clamp() and viewport-relative classes
4. grep confirms zero instances of text-sm, text-xs, text-xl, text-2xl in company-display.tsx
5. grep confirms h-1/3 present for artist card wrappers
6. grep confirms carousel logic (currentPage, totalPages, setInterval) is preserved
</verification>

<success_criteria>

- Company page has 50/50 two-column layout (grid-cols-2)
- Left half: 2x3 metric cards filling container height
- Right half: performer cards stacked at exactly 1/3 height each
- Carousel auto-rotates when >3 performers
- All text uses clamp() — no fixed Tailwind text size classes
- Cards are always 1/3 height even with fewer than 3 performers (per D-15)
  </success_criteria>

<output>
After completion, create `.planning/phases/02-tv-grid-layout/02-03-SUMMARY.md`
</output>
